# تقرير فحص مستندك — 2026-09-11

فحص شامل للتطبيق ولوحة الأدمن: تشغيل محلي وتجربة كل الصفحات يدويًا (إنشاء عميل/عقد/فاتورة/مشروع/مستند، الإرسال للتوقيع، التوقيع من صفحة العميل، الصفحة العامة، الأدمن، الجوال، الوضع الداكن) + مراجعة كاملة لكود السيرفر والواجهة.

**حساب الاختبار المحلي:** `qa-local@mostandoc.test` / `QaLocal#2026` (سوبر أدمن، باقة برو مضافة يدويًا في قاعدة البيانات المحلية).

---

## الخلاصة التنفيذية

| المستوى | العدد | أهم ما فيه |
|---|---|---|
| حرج | 7 | إرسال الفاتورة معطّل بالكامل، أي أدمن يرفع نفسه سوبر أدمن، الحساب الموقوف يقدر يدخل عادي، XSS في صفحة التوقيع، الويب هوك ممكن ما يفعّل الاشتراك المدفوع |
| عالي | 14 | تعديل أي حقل من الـ API بدون تحقق (mass assignment)، ملفات المستندات المحذوفة تبقى قابلة للتحميل، المستند الموقّع يُحفظ على قرص Railway المؤقت ويضيع، `drizzle-kit push --force` يشتغل مع كل نشر، لا يوجد code splitting (حزمة 2.3MB) |
| متوسط | 25+ | لا تحقق من الإيميل/الأرقام السالبة/التواريخ، أرقام الفواتير تتكرر، لا Foreign Keys، لوحة الإيرادات في الأدمن أرقام عشوائية، مشاكل RTL وتنسيق أرقام وتواريخ |
| تقني | — | `npm run check` فيه **98 خطأ TypeScript** والبناء ما يتحقق منها. 12 مكتبة متأخرة إصدارات كبيرة |

**الحكم العام:** الأساس جيد (Drizzle + TanStack Query + فصل الطبقات + Zod في الأماكن الحساسة + Stripe webhook موثّق + bcrypt + جلسات Postgres). المشاكل تتركز في: (1) غياب التحقق من المدخلات في الـ CRUD، (2) خط سير التوقيع الإلكتروني، (3) الفوترة، (4) الدين التقني (TS معطّل، ملفات ضخمة، كود ميت).

---

## أولًا: ما وجدته بالتجربة الفعلية (Live)

### حرج
1. **إرسال الفاتورة للعميل يفشل دائمًا** — `server/routes.ts:817` يستدعي `storage.getUser()` وهي غير موجودة (`TypeError: storage.getUser is not a function`). كل ضغطة على أيقونة الإرسال تعطي 500. كذلك يقرأ `user.fullName / user.username` وهي أعمدة `profiles` مو `users`.
2. **رسالة الخطأ تظهر بصيغة JSON خام للمستخدم:** `500: {"message":"فشل في إرسال الفاتورة"}` (invoices.tsx — الـ toast يعرض نص الاستجابة كما هو).
3. **إيميل الفاتورة يوجّه العميل لرابط `/dashboard/invoices`** وهي لوحة تحكم المرسل نفسه، العميل ما يقدر يفتحها. لا يوجد رابط عام للفاتورة ولا PDF مرفق.

### عالي
4. **زر "حفظ البروفايل" في صفحتي العامة مغطّى بزر واتساب العائم** — الضغط عليه يفتح واتساب بدل الحفظ (تحققت بـ `elementFromPoint`). نفس الزر يغطي أزرار أخرى أسفل يسار الصفحات.
5. **فتح العقد في المحرر ينشئ مستند جديد مع كل ضغطة** (contracts.tsx:148) — ضغطت مرتين = مستندين بنفس الاسم، بدون ربط بالعقد، وقائمة المستندات ما تتحدث.
6. **قالب العقد لا يُملأ ببيانات العقد** — عند فتح العقد في المحرر تظهر الفراغات `_______` بدل اسم العميل/القيمة/التواريخ اللي أدخلتها للتو.
7. **إرسال للتوقيع على الباقة المجانية:** السيرفر يرجع `403 {upgrade:true}` لكن المحرر يعرض "فشل إرسال المستند" فقط بدون توجيه للترقية، والزر ظاهر بدون أي إشارة إنه مقفول.
8. **بعد التوقيع، صفحة العميل ما تعرض التوقيع ولا اسم الموقّع ولا التاريخ** — تظهر "تم التوقيع بنجاح" والمستند بنفس الفراغات `التوقيع: ______`. صفر صور في الصفحة. ولا يوجد زر تحميل النسخة الموقّعة للعميل.
9. **فرق 3 ساعات بين توقيتين لنفس التوقيع:** `documents.signed_at = 17:23` و `document_signatures.signed_at = 20:23`. الأعمدة `timestamp without time zone` وأحدها يُكتب من JS (UTC) والآخر من `now()` في Postgres.
10. **المحرر النصي غير قابل للاستخدام على الجوال:** شريط العناصر يفتح تلقائيًا ويأخذ 40% من العرض، والتولبار يلتف على 3 أسطر.
11. **لوحة الأدمن على الجوال:** القائمة الجانبية ثابتة `w-56` تأخذ 60% من الشاشة، الجداول غير قابلة للقراءة.

### متوسط
12. **لا يوجد تحقق من الإيميل في نموذج العميل** — قبل `not-an-email` بدون اعتراض (لا في الواجهة ولا السيرفر).
13. **العقد يقبل تاريخ نهاية قبل تاريخ البداية** (2026-09-01 → 2026-08-01).
14. **الفاتورة تقبل كمية سالبة** (-2) وبنود فاضية.
15. **الفاتورة تقبل الحفظ بدون تاريخ استحقاق** ثم يظهر "-" في الجدول ولا تتحول لـ "متأخرة" أبدًا.
16. **الإجمالي قبل الحفظ يظهر بـ 3 خانات عشرية** (٤٬٢٥٩٫٢٣٢) — التقريب يصير في قاعدة البيانات فقط.
17. **اسم المستخدم للصفحة العامة بدون أي تحقق** — قبل "أمين اختبار" (عربي + مسافة) كرابط `/p/...`.
18. **الحقول في نموذج "صفحتي العامة" مقلوبة على القارئ:** أول حقل (الاسم الكامل بصريًا) يُحفظ كـ username والعكس — تأكد من ترتيب الـ inputs مقابل الـ labels.
19. **placeholder التاريخ معكوس:** يظهر `موي/رهش/ةنس` في كل حقول `type="date"` (العقود/الفواتير/المشاريع/الأدمن). `dir="ltr"` ما حلّها.
20. **تناقض الأرقام:** بطاقات العقود والفواتير بأرقام هندية `١٥٬٠٠٠ SAR` والتواريخ بأرقام لاتينية `2026-09-01`، وصفحة الإعدادات بأرقام لاتينية. وصفحة المستندات تعرض التاريخ `١١‏/٩‏/٢٠٢٦` بينما الفواتير `2026-09-11`.
21. **`toLocaleDateString("ar-SA")` يعطي تقويم هجري افتراضيًا** في الداشبورد/العملاء/المستندات/التوقيع، بينما بقية الصفحات ميلادي.
22. **الأدمن > الاشتراكات يعرض أسعار غير موجودة:** "ستارتر 29 / برو 59 / بيزنس 99" بينما الباقات الفعلية في `shared/plans.ts`: مجاني / مبتدئ 29 / محترف من 100. ويقدر الأدمن يعيّن باقة `business` غير معرّفة (تعطي صلاحيات غير محدودة).
23. **نموذج "إرسال للتوقيع" في المحرر النصي** يستخدم `<select>` أصلي و overlay يدوي بدل Dialog/Select الموحدة — Escape ما يقفله، وما فيه focus trap.
24. **حذف بدون تأكيد:** المستندات، مهام المشاريع، الخدمات، معرض الأعمال، مكتبة المحتوى، إيقاف حساب في الأدمن، وإرسال إشعار/إيميل لكل المستخدمين بضغطة واحدة.
25. **روابط `/privacy` و `/terms` في الفوتر تعطي 404** — مع وجود Stripe checkout هذي مشكلة امتثال.
26. **البحث العام (⌘K) يشتغل** لكن النتائج توجّه لصفحة القائمة بدون فتح العنصر نفسه.
27. **الداشبورد يعرض 0 أثناء التحميل** بدل skeleton (`isLoading` معرّف وغير مستخدم).
28. **console warnings:** `Missing Description or aria-describedby for DialogContent` في كل الـ dialogs.

### ما اشتغل صح
- تسجيل الدخول/الخروج، الـ CRUD الأساسي، فلاتر الحالة، الـ pagination، الوضع الداكن (ممتاز)، البحث العام، القوالب، الصفحة العامة `/p/:username`، صفحة التوقيع (العرض والرسم)، الأدمن (نظرة عامة/المستخدمون/المستندات/التحليلات/السجل)، حساب الضريبة 15% صحيح.

---

## ثانيًا: السيرفر (server/)

### حرج
- **C1. إرسال الفاتورة معطّل** — `routes.ts:817` (تفصيل أعلاه). الحل: `getUserById()` + `storage.getProfile()`.
- **C2. أي أدمن يقدر يرفع نفسه سوبر أدمن** — `routes.ts:1952` يقبل `role` بدون قائمة مسموحة ولا فحص للمستهدف. الحل: `isSuperAdmin` + `z.enum(["user","admin"])` + منع تعديل الذات والسوبر أدمن.
- **C3. الحساب الموقوف ما ينحجب أبدًا** — `isSuspended` ما يُقرأ في login ولا OAuth ولا `isAuthenticated` (`customAuth.ts:44`). الحل: فحصه عند الدخول وفي الـ middleware.
- **C4. Stripe webhook يقرأ `subscription.current_period_start`** (`routes.ts:1322`) وهي انتقلت لـ `items.data[0]` في API 2025-03-31+. لو الـ endpoint في لوحة Stripe على إصدار حديث → `Invalid Date` → 500 → الاشتراك المدفوع يبقى free. تحقق من إصدار الـ webhook في لوحة Stripe فورًا.

### عالي
- **H1. Mass assignment في كل PATCH** — `routes.ts:639, 704, 781, 898, 1518, 1668` تمرر `req.body` كامل للـ storage. يقدر المستخدم يغيّر `userId` (ينقل سجلاته لحساب ثاني)، أو يزوّر `status:"signed"` و `signedAt` و `shareToken` على مستند. الحل: `insertXSchema.partial().omit({userId:true}).strict()` — الـ schemas موجودة أصلًا في `shared/schema.ts` وغير مستخدمة.
- **H2. Stored XSS في التوقيع** — `routes.ts:1808` يكتب `signatureData` بدون تحقق داخل HTML يُخدَم من نفس الدومين (`/uploads/signed/*.html`) و CSP معطّل. الموقّع المجهول يقدر يحقن سكربت يشتغل بجلسة صاحب المستند. الحل: regex على `data:image/(png|jpeg);base64,...` + `xss()` على المحتوى + تقديم الملف كـ attachment أو توليد PDF حقيقي.
- **H3. حذف المستند لا يحذف `document_files`** (`storage.ts:716, 1027`) و `GET /api/documents/:id/file` عام ويقرأ `document_files` قبل التحقق من وجود المستند. النتيجة: المستند المحذوف يبقى قابل للتحميل للأبد، وكل رفع محذوف يسرّب حتى 27MB في قاعدة البيانات. والـ header `Cache-Control: public, max-age=31536000` على ملفات خاصة.
- **H4. `drizzle-kit push --force` في `railway.json` مع كل نشر** — بدون مجلد migrations. أي إعادة تسمية عمود = DROP + ADD على قاعدة الإنتاج بدون مراجعة. (هذا نفس السبب اللي خلّى قاعدتي المحلية ناقصة عمود `documents.notes` اليوم.) الحل: `drizzle-kit generate` + `migrate` كخطوة release منفصلة.
- **H5. الإيرادات في لوحة الأدمن أرقام مفبركة** — `storage.ts:1097` يستخدم `Math.random()` للأشهر السابقة، والأسعار hardcoded `{starter:29, pro:59, business:99}` مختلفة عن `shared/plans.ts`، و `getAdminGrowthStats` يرجع `revenue: 0` دائمًا.
- **H6. المستند الموقّع يُكتب على قرص Railway المؤقت** (`uploads/signed/*.html`) — يضيع مع كل نشر أو إعادة تشغيل، والـ DB يبقى يقول `signed` مع `fileUrl` مكسور. وهو HTML مو PDF. هذا هو الملف اللي تحتاجه قانونيًا في أي نزاع.
- **H7. صلاحيات الباقة تتجاهل `status` و `currentPeriodEnd`** — `past_due` يبقى بمزايا برو، والباقة المجهولة تسقط على **pro غير محدود** (`shared/plans.ts:92`) بدل free.

### متوسط
- **M1.** `/api/pdf-preview` (`routes.ts:1360`) بدون auth + SSRF + path traversal + `execSync("pdftoppm")` — ولا أحد يستخدمه من الواجهة. احذفه.
- **M2.** `/api/documents/sign/:token` يرجع للموقّع `userId, clientId, recipientEmail, shareToken, notes` (ملاحظات المالك الخاصة).
- **M3.** `PUT /api/documents/:id/fields` يحذف الحقول قبل التحقق من الـ body — payload خاطئ = الحقول راحت.
- **M4.** إنشاء/تعديل الفاتورة بدون transaction، والمجاميع تجي من العميل بدون إعادة حساب في السيرفر، وإيميل الفاتورة يحسب الضريبة 15% ثابتة متجاهلًا `vatRate` المخزّن.
- **M5.** أرقام الفواتير `INV-${count+1}` — احذف فاتورة أو افتح تبويبين = رقم مكرر. لا يوجد unique index.
- **M6.** `PATCH /api/auth/user` يقدر يصفّر الإيميل (`email || null`)، ويغيّر الإيميل بدون إعادة تحقق، و duplicate يعطي 500.
- **M7.** لا يوجد أي Foreign Key في الـ schema. حذف مستخدم من الأدمن يترك عملاءه وعقوده وفواتيره ومستنداته وجلساته يتيمة للأبد.
- **M8.** رسائل الأخطاء الداخلية (أسماء أعمدة Postgres) ترجع للعميل — `index.ts:87` وأغلب مسارات الأدمن.
- **M9.** الـ logger يطبع body كل استجابة API كاملة في stdout (بيانات دخول، مستندات كاملة مع base64 توقيعات، قوائم PII).
- **M10.** Rate limiting نصفه ميت: `generalLimiter` مركّب قبل `setupCustomAuth` فـ `req.session` دائمًا undefined؛ limiter واحد مشترك بين login/register/verify/forgot ومطبّق مرتين على forgot؛ وفي وضع cluster كل worker له عدّاد مستقل.
- **M11.** تجربة مجانية 14 يوم مع كل checkout — إلغاء ثم اشتراك = تجربة جديدة بلا نهاية.
- **M12.** endpoint إيميل الأدمن يرسل HTML خام لأي عناوين — open relay على دومين البراند.
- **M13.** مسارات ترحيل Bubble ما زالت حية في الإنتاج (`/api/admin/migrate/*`) وتبني روابط إعادة التعيين من `REPLIT_DOMAINS` (مكسورة على Railway).
- **M14.** لا Zod على أي POST/PATCH أساسي (clients/contracts/invoices/projects/profile/services/portfolio/admin templates/coupons/settings/tracking-scripts).
- **M15.** لا indexes على أي عمود child: `invoice_items.invoice_id`, `project_tasks.project_id`, `document_fields.document_id`, `document_signatures.document_id`, `contracts.client_id`, `invoices.client_id/due_date/status`...
- **M16.** الكاش `Map` محلي لكل worker — `cache.invalidate()` يمسح worker واحد من 4، الباقي يخدم بيانات قديمة 30 دقيقة.

### منخفض
- لا `session.regenerate()` عند الدخول ولا عند الانتحال. التحقق من الإيميل ما يُفرض أبدًا. ربط حسابات OAuth بالإيميل بدون فحص `verified`. `require("passport-apple")` في ملف ESM (ينكسر لحظة إضافة متغيرات Apple). Multer يثق بـ MIME من العميل + `image/jpg` مو نوع حقيقي. `getDocuments`/`getContentLibrary` بدون حد. `/api/health` يكشف الذاكرة والـ pool للعموم. موديل Claude ثابت `claude-sonnet-4-20250514`. `noreply@resend.dev` كـ fallback للمرسل في الإنتاج. `express.json({limit:"30mb"})` عام. جداول التوكنات ما تُنظّف أبدًا.

### نموذج البيانات
- **لا FKs ولا cascade** (أهم إصلاح schema).
- أعمدة الحالة نص حر — استعلام تحليلات الأدمن يعدّ `invoices.status='pending'` و `projects.status='active'` (`routes.ts:2512`) بينما التطبيق يكتب `draft/sent/paid` و `not_started/in_progress` → هذي الأرقام صفر دائمًا.
- `document_files.file_data` كـ base64 في `text` (+33% حجم) — استخدم object storage (المكتبة موجودة).
- التوقيتات `timestamp without time zone` مع خلط JS/Postgres (الفرق 3 ساعات اللي شفته).

---

## ثالثًا: الواجهة (client/src)

### حرج/عالي
- **F1. لا code splitting** — `App.tsx` يستورد 22 صفحة eagerly (TipTap + pdfjs + html2canvas + jsPDF + recharts + الأدمن). حزمة واحدة **2.3MB** لكل زائر حتى صفحة `/auth`. الحل: `React.lazy` لكل route + dynamic import لمكتبات PDF.
- **F2. `document-editor.tsx:119`** — الـ PDF يُعاد تحميله وتحليله وبناء iframe جديد مع كل حركة ماوس أثناء سحب حقل (`onLoad` inline في deps). و `URL.createObjectURL` ما يُحرَّر.
- **F3. `document-editor.tsx:175`** يقرأ `/api/clients` كمصفوفة بينما الاستجابة `{data,...}` → قائمة "ربط بعميل" ما تظهر أبدًا. ونفس المفتاح `["/api/clients"]` مستخدم بـ queryFn مختلف في المحرر النصي مع `staleTime: Infinity` → تعارض كاش.
- **F4. إحداثيات حقول التوقيع بالبكسل المطلق** في canvas عرضه 800، وصفحة التوقيع تعرضها في حاوية 896px بـ iframe ثابت 900px → الحقل في مكان غلط، وعلى الجوال خارج الشاشة.
- **F5. المتغيرات الذكية `{{اسم_الشركة}}` ما تُحل أبدًا** — `text-document-editor.tsx:242` يقرأ `profile.company/address/phone` والأعمدة الفعلية `companyName/companyAddress/phonePublic`.
- **F6. سكربتات التتبع تتجاهل إعداد المكان** — `tracking-scripts-injector.tsx` يطلب `placement=all` دائمًا، فسكربت "الرئيسية فقط" يشتغل في الداشبورد وصفحة التوقيع. و GTM noscript يُحقن مرتين.
- **F7. Stored XSS في `sign-document.tsx:184,248`** — `dangerouslySetInnerHTML` على `doc.content` بدون تعقيم والسيرفر ما يعقّم `content` في PATCH. نفس الشي لمخرجات AI في المحرر.
- **F8. كود ميت في الحزمة:** `pages/admin-dashboard.tsx` (503 سطر، غير مستورد)، `ObjectUploader.tsx` + Uppy CSS، `platform-banner.tsx` (ميزة "بانر المنصة" موجودة في الأدمن وما تظهر للمستخدمين أبدًا)، `fillFieldsInHtml`.

### متوسط
- Autosave في المحرر يعمل `invalidateQueries(["/api/documents"])` كل 2 ثانية → يعيد جلب القائمة والمستند كامل أثناء الكتابة.
- المحرر يستخدم `h-screen` + `sticky top-[53px]` داخل layout له header → الصفحة أطول من الشاشة بـ 50px.
- الـ redirect للدخول يضيّع الرابط الأصلي (`returnTo`) — روابط الإيميل للمستند تنتهي بالداشبورد.
- `use-plan-limits` بـ `staleTime: 30s` وما يُبطل بعد الإنشاء → المستخدم عند 4/5 يضيف واحد، الزر يبقى مفعّل، الـ POST يعطي 403 والـ dialog يبقى مفتوح بدون رسالة (mutations بدون `onError` في clients/contracts/invoices/projects/my-page).
- `window.prompt` للروابط والصور واسم المكتبة (LTR، بدون تحقق).
- `fetch` بدون `try/catch` ولا `res.ok` في `invoices.tsx:133-155`.
- 5 نسخ من نفس هيكل صفحة CRUD (state + pagination + 3 mutations + dialog حذف + spinner) — يحتاج `usePaginatedQuery` + `ConfirmDialog` + `StatusBadge`.
- أسعار الباقات مكررة في `landing.tsx` و `settings.tsx` و 3 صفحات أدمن و `storage.ts` — المصدر الوحيد المفروض `shared/plans.ts`.
- اللاندنج تحمّل خط Tajawal ثاني و 300 سطر CSS داخل `<style>` بدون Tailwind ولا dark mode، وإحصائيات ثابتة "500+ شركة، 15,000+ مستند".
- 130+ استخدام لـ `ml-/mr-/pl-/pr-/left-/right-` بدون `ms-/me-/ps-/pe-`.
- 33 زر أيقونة بدون `aria-label`.
- `not-found.tsx` بألوان ثابتة تتجاهل الوضع الداكن.
- `public-profile.tsx` روابط `target="_blank"` بدون `rel="noopener"`.
- `admin-settings.tsx` الدومين الافتراضي `mustandak.com` بدل `mostandoc.com`.

---

## رابعًا: الدين التقني والمكتبات

- **`npm run check` = 98 خطأ** (84 في `routes.ts`، 3 في `cache.ts`، 3 في كل من المحررين وصفحة التوقيع). أغلبها Express 5 (`req.params.id: string | string[]`) لكن بينها أخطاء حقيقية: `getUser` (C1)، `profile.businessName` (اسم المرسل في إيميل التوقيع دائمًا "مستخدم مستندك")، `getDocument` بمعامل واحد (fallback رابط Bubble ما يشتغل)، `AuditAction` ناقصة. `tsconfig` يحتاج `"target": "es2022"`. والأهم: **البناء (esbuild) ما يتحقق من الأنواع** فهذي الأخطاء تروح للإنتاج.
- **مكتبات غير مستخدمة:** `memorystore`, `ws`, `passport-local`, `google-auth-library`, `openid-client`, `@tailwindcss/vite@4` (جنب tailwind 3), و `@types/*` في dependencies. `node-fetch` مستخدم وغير مُعلن. `server/replit_integrations/` و `server/migration.ts` كود ميت.
- **إصدارات متأخرة:** TypeScript 5.6 → 7.0، Zod 3 → 4 (يغيّر `.errors` → `.issues`)، Stripe 20 → 22، Drizzle 0.39 → 0.45، React 18 → 19، Tailwind 3 → 4، Vite 7 → 8، recharts 2 → 3، lucide 0.45 → 1.45، date-fns 3 → 4.
- **لا اختبارات، لا ESLint/Prettier، لا CI.**

---

## خامسًا: تحسينات منتج مقترحة (من التجربة)

1. **صفحة عامة للفاتورة** `/invoice/:token` مع PDF — بدلًا من توجيه العميل للداشبورد.
2. **ربط العقد بالمستند** (`contracts.documentId`) وملء القالب ببيانات العقد تلقائيًا.
3. **صفحة ما بعد التوقيع:** عرض التوقيع + الاسم + التاريخ + IP داخل المستند، وزر تحميل النسخة الموقّعة، وإرسال نسخة للطرفين.
4. **تحقق هوية الموقّع** (OTP على الإيميل) — حاليًا أي شخص معه الرابط يوقّع بأي اسم.
5. **قفل المستند بعد الإرسال** — المالك حاليًا يقدر يعدّل المحتوى بعد ما أرسله للتوقيع.
6. **تفاصيل المشروع** — البطاقة ما تعرض الميزانية ولا المهام رغم وجود جدول `project_tasks`.
7. **مصدر واحد للباقات والأسعار** (`shared/plans.ts`) في كل مكان.
8. **`formatDate` / `formatMoney` موحّدة** (ميلادي + أرقام لاتينية أو هندية — قرار واحد).
9. **onboarding بعد التسجيل** (أكمل ملفك → أضف أول عميل → أنشئ أول مستند).
10. **إخفاء زر واتساب العائم** داخل المحرر وصفحات النماذج، أو نقله لليمين فوق الـ FABs.

---

## سادسًا: خطة العمل المقترحة

### المرحلة 1 — إصلاحات فورية ✅ (نُفّذت 2026-09-11)
- [x] C1 إرسال الفاتورة — يستخدم `getUserById` + `profiles` والمبلغ المخزّن، وأُزيل رابط الداشبورد من إيميل العميل.
- [x] C2 ترقية الأدمن — Zod على `PATCH /api/admin/users/:id`: تغيير الدور للسوبر أدمن فقط، منع تعديل الذات/السوبر أدمن، الباقات محصورة في `free|starter|pro`.
- [x] C3 الحسابات الموقوفة — فحص `isSuspended` في login و OAuth و `isAuthenticated` (كاش 60 ثانية يُبطل عند التعديل) و `isAdmin`/`isSuperAdmin`.
- [x] C4 Stripe webhook — يقرأ `items.data[0].current_period_*` مع fallback للشكل القديم، وحماية من `Invalid Date`.
- [x] رسالة JSON الخام — `ApiError` في `lib/queryClient.ts` يستخرج `message` العربية و `upgrade`؛ المحرر يعرض رسالة الترقية بدل "فشل إرسال المستند".
- [x] زر واتساب يغطي الحفظ — `pb-24` على `main` وإخفاء الزر داخل المحررات.
- [x] Zod على إنشاء العقود/الفواتير/المشاريع (يمنع `id`/`userId` من العميل).
- [x] `session.regenerate()` عند الانتحال؛ صفحة الدخول تعرض سبب فشل OAuth/الإيقاف من `?error=`.
- [x] `tsc` صفر أخطاء (`target: es2022` + دالة `param()` لمعاملات Express 5) و `npm run build` يفشل الآن لو فيه خطأ أنواع.
- [ ] **يدوي:** تحقق من إصدار API الخاص بالـ webhook endpoint في لوحة Stripe.

### المرحلة 2 — الأمان والبيانات ✅ (نُفّذت 2026-09-11)
- [x] Zod على كل POST/PATCH (عملاء، عقود، فواتير وبنودها، مشاريع، مهام، خدمات، معرض، بروفايل، رسائل التواصل، المستندات، مكتبة المحتوى، حساب المستخدم، قوالب/كوبونات/إعدادات/سكربتات الأدمن) مع حذف `id`/`userId` من المدخلات (H1, M14). تحقق إضافي: إيميل صالح، نهاية العقد بعد بدايته، كمية البند > 0، اسم مستخدم بصيغة رابط.
- [x] `PATCH /api/auth/user` ما يصفّر الإيميل، وتغيير الإيميل يتطلب إعادة تحقق ويرفض المكرر (M6).
- [x] FKs مع cascade/set null على كل الجداول + 20 index ناقص + unique(user_id, invoice_number) (M7, M15, D3).
- [x] migrations حقيقية: `migrations/0000_baseline.sql` (idempotent) + `0001` (تنظيف اليتامى ثم القيود). `railway.json` يشغّل `node dist/migrate.cjs` بدل `push --force` (H4).
- [x] خط التوقيع: التوقيع يُقبل فقط كـ PNG/JPEG data URI؛ محتوى المستند يُعقَّم بـ xss (قائمة سماح TipTap) عند الحفظ وعند التوقيع؛ النسخة الموقّعة تُخزَّن في `documents.signed_content` وتُقدَّم من `/api/documents/:id/signed` (المالك) و `/api/documents/sign/:token/signed` (الموقّع) مع CSP صارم؛ أُزيل `express.static("/uploads")` (H2, H6, F7).
- [x] المستند الموقّع غير قابل للتعديل (409)، والإرسال يتطلب بريد مستلم.
- [x] `GET /api/documents/:id/file` يتحقق من وجود المستند أولًا و `Cache-Control: private` (H3، والحذف يسقط `document_files` بالـ cascade).
- [x] صفحة التوقيع العامة ترجع projection بدون `userId/clientId/recipientEmail/shareToken/notes` (M2)، وبعد التوقيع تعرض النسخة الموقّعة مع رابط تحميل.
- [x] حذف `/api/pdf-preview` و `/api/admin/migrate/*` و `server/migration.ts` و `server/replit_integrations/` وصفحة `admin-migrate` (M1, M13).
- [x] استعلام تحليلات الأدمن يعدّ الحالات الفعلية (`sent`/`in_progress`) (D2).
- [ ] مؤجل للمرحلة 3: إعادة حساب مجاميع الفاتورة في السيرفر + transaction + رقم فاتورة بتسلسل (M4, M5). مؤجل للمرحلة 4: نسب مئوية لحقول التوقيع (F4).
- ملاحظة: فرق الـ 3 ساعات في `signed_at` يظهر محليًا فقط (JS يكتب UTC و `now()` بتوقيت الجلسة)؛ على Railway (UTC) القيمتان متطابقتان.

### المرحلة 3 — الفوترة والباقات (أسبوع)
- Webhook: قراءة `items.data[0].current_period_*` (C4)، احترام `status`/`currentPeriodEnd` (H7)، تجربة واحدة فقط (M11)، إيرادات حقيقية في الأدمن (H5)، مصدر واحد للأسعار.
- الفواتير: حساب المجاميع في السيرفر + transaction + رقم فريد بتسلسل + منع الكمية السالبة + تاريخ استحقاق إلزامي + صفحة فاتورة عامة.

### المرحلة 4 — الواجهة والأداء (أسبوع–أسبوعين)
- `React.lazy` لكل الصفحات + dynamic import للـ PDF/charts (F1).
- إصلاح المحررين (F2–F5) وتوحيد `PdfRenderer` بنسب مئوية.
- استخراج `usePaginatedQuery` / `ConfirmDialog` / `StatusBadge` / `formatDate` / `formatMoney`.
- تأكيدات الحذف، `onError` لكل mutation، إبطال `subscription/limits`، `returnTo` بعد الدخول، صفحات `/privacy` و `/terms`.
- جوال: المحرر (sidebar مغلق افتراضيًا + toolbar قابل للطي) والأدمن (drawer).

### المرحلة 5 — البنية (مستمر)
- تقسيم `routes.ts` (2770 سطر) لملفات حسب المورد، و `storage.ts` حسب المجال.
- Vitest للـ storage والـ API الأساسية + GitHub Actions (check + build + test).
- ESLint/Prettier، structured logging بدون أجسام الاستجابات (M9)، ترقية المكتبات تدريجيًا (Zod 4 و Stripe 22 أولًا).
